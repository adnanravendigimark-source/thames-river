// Single source of truth for the site's public URL — used by the root
// layout (metadataBase, canonical/OG URL resolution), sitemap.xml, and
// robots.txt.
export const SITE_URL = "https://www.penapalaceentry.com";
